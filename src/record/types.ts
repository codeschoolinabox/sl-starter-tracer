/**
 * @file Internal types for your tracer's record module.
 *
 * TODO: Replace these placeholders with your actual types.
 *   - RawStep: your engine's raw output shape before adapting to StepCore
 *   - TracerOptions: your options type — must match options.schema.json
 */

/** Your engine's raw step shape before adapting to StepCore. Replace this. */
export type RawStep = Record<string, unknown>;

/** Your tracer's options type. Must match options.schema.json. Replace this. */
export type TracerOptions = Record<string, unknown>;
